/**
 * Константа `MOBILE_VIEWPORT_MAX_PX`, используемая как mobile viewport max px.
 */
export const MOBILE_VIEWPORT_MAX_PX = 768;

export const MOBILE_VIEWPORT_MEDIA_QUERY = `(max-width: ${MOBILE_VIEWPORT_MAX_PX}px)`;
